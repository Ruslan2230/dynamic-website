mutation CreatePage($pageDetails: PageDetailsInput!) {
    createPage(pageDetails: $pageDetails) {
        id
        title
        layout {
            headerStyle
            footerStyle
        }
    }
}